 // const cn = (...args: string[]) => args.filter(Boolean).join(" ")

// Notes/Menu.tsx
export type Setup = {
    check: boolean;
}
export type Setup2 = {
    check: boolean;
}

// Notes/AddList.tsx
export type BooleanCheck = {
    handleClick: (bools: boolean) => void
}